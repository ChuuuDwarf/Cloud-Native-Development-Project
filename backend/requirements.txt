# LIMS backend — runtime dependencies
# Generated and curated; refresh with `pip-compile` or `uv pip compile` if you change.

# --- Web framework ---
fastapi==0.128.8
uvicorn[standard]==0.39.0
starlette==0.49.3
python-multipart==0.0.12

# --- Validation / settings ---
pydantic==2.13.3
pydantic-core==2.46.3
pydantic-settings==2.6.1
email-validator==2.2.0

# --- DB ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# --- Auth ---
python-jose[cryptography]==3.3.0
# Direct bcrypt — passlib 1.7.4 (unmaintained since 2020) is incompatible with
# bcrypt 4.x (it reads bcrypt.__about__ which was removed). Going direct keeps
# the dependency tree shorter and works with current bcrypt.
bcrypt==4.2.1

# --- Background jobs / cache / pub-sub ---
celery==5.4.0
redis==5.2.0

# --- Realtime ---
sse-starlette==2.1.3

# --- Email ---
fastapi-mail==1.4.2

# --- HTTP client (used by some integrations + test client) ---
httpx==0.27.2

# --- Misc runtime helpers carried over ---
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.12.1
click==8.1.8
colorama==0.4.6
exceptiongroup==1.3.1
h11==0.16.0
idna==3.13
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.39.0
psycopg[binary]==3.3.2
pytest==8.4.2
pytest-cov==6.2.1
ruff==0.14.9
mypy==1.17.1
